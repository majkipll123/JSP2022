#print("%d, %d" % (5,10))
f"{5:5d},{10:d}"
#list komprehenciotn wazny pythonowy 
#petle w pythonie sa dynamiczne i to po czym iteruje nie musi byc intigerem
for element in array:
    eklem

##
for i,n in zip (imiona, nazwiska)
lista.append("{i} {n}")

##
lista=[f"{i} {n}" for i,n in zip(imiona,nazwiska)]

